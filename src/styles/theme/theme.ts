import ThemeInterface from '../interface/Theme';

const theme: ThemeInterface = {
  title: 'default',
  colors: {
    black: '#000',
    gray: '#f5f5f5',
    white: '#fff',
    primary: '#000',
    secondary: '#000',
    tertiary: '#000',
  },
};

export default theme;
